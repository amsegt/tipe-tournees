# Dependances figees pour reproductibilite (Python 3.13, Windows).
# Mode hypothese : seules matplotlib (+ numpy) sont requises.
# Mode OSM (contre-epreuve) : osmnx + scikit-learn (nearest_nodes) + folium (carte).
#
# Reconstruction de l'environnement :
#   python -m venv .venv
#   .venv\Scripts\pip install -r requirements.txt
#
# Versions verrouillees (telles qu'utilisees pour produire results.csv,
# results_osm.csv, road_graph.graphml et les figures) :

# --- calcul & trace (mode hypothese et commun) ---
numpy==2.4.6
matplotlib==3.10.9
networkx==3.6.1

# --- mode OSM (reseau routier reel) ---
osmnx==2.1.0
scikit-learn==1.8.0
geopandas==1.1.3
shapely==2.1.2
requests==2.34.2
folium==0.20.0

# --- generation des livrables (rapports PDF / diapos PPTX) ---
python-pptx==1.0.2
pillow==12.2.0
